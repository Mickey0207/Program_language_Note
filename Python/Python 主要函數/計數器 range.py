# range(start, stop, step)
# start     計數開始，預設為 0
# stop      計數結束值，但不包含 stop
# step      計數間隔，預設為 1

range(10)           # 從 0 開始到 9
range(1, 11)        # 從 1 開始到 10
range(0, 30, 5)     # 間隔為 5
range(0, 10, 3)     # 間隔為 3
range(0, -10, -1)   # 負數
range(0)
range(1, 0)
